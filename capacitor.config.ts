import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "namtar-rules",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
